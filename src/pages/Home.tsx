import React from 'react';

// Import stores

// Import assets

// Import components

// Import features

// Import types

// Import libs

interface HomeProps { }


const Home: React.FC<HomeProps> = ({ }) => {
  // Render the component
  return (
    <main>
    </main>
  );
};

export default Home;